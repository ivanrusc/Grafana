# Grafana
# instal·lar Grafana + influxdb v1.8, en una Raspberry Pi amb docker-compose


$ cd docker
$ mkdir grafana
$ cd grafana
$ mkdir data_grafana
$ mkdir data_influxdb
$ nano docker-compose.yml
$ docker-compose up -d
